import { FocusOnInitDirective } from './focus-on-init.directive';

describe('FocusOnInitDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusOnInitDirective();
    expect(directive).toBeTruthy();
  });
});
